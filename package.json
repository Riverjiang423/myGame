{
  "name": "mygame",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js",
    "install": "node -e \"console.log('skip native build on install; run npm run build:libzt when needed')\"",
    "build:libzt": "node-gyp rebuild",
    "dist:prepare": "node scripts/prepare-distribution.js",
    "preflight": "node scripts/release-check.js",
    "release:check": "node scripts/release-check.js",
    "test": "node --test --test-isolation=none \"tests/**/*.test.js\"",
    "test:unit": "node --test --test-isolation=none \"tests/unit/**/*.test.js\"",
    "test:integration": "node --test --test-isolation=none \"tests/integration/**/*.test.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "node-addon-api": "^8.2.1",
    "node-gyp": "^11.2.0",
    "socket.io-client": "^4.8.3"
  }
}
