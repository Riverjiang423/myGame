# 功能说明文档

## 一、项目简介

这是一个可以让多人一起玩的网页小游戏联机系统。  
它解决了一个核心问题：**不用复杂部署，用户打开网页就能快速建房、分享链接、和朋友一起玩**。

---

## 二、核心功能列表

- **功能名称：多人房间联机**
  - **功能描述：**  
    支持创建房间、加入房间、离开房间。系统会自动维护房间成员和房主身份。
  - **使用场景：**  
    你想和朋友快速开一局，不想折腾复杂服务器或手工同步状态。

- **功能名称：双游戏模式（扫雷 + 德州扑克）**
  - **功能描述：**  
    一个房间里可以选择不同游戏玩法：  
    - 扫雷：多人同房间进行互动操作  
    - 德州扑克：支持完整的对局流程与结算展示
  - **使用场景：**  
    同一群人想在一个系统里切换不同小游戏，不想换平台。

- **功能名称：默认房间与分享链接**
  - **功能描述：**  
    服务启动后会自动准备默认房间，并给出推荐分享地址。  
    用户可一键复制完整链接（自动带房间号），直接发给朋友。
  - **使用场景：**  
    房主想“开机即开房”，减少手动创建和拼接链接的步骤。

- **功能名称：断线重连与房间保留**
  - **功能描述：**  
    玩家临时断线后，系统会在一段时间内保留会话，方便重新连接恢复。  
    超过保留时间会按规则清理。
  - **使用场景：**  
    网络波动、浏览器刷新、临时掉线时，尽量不中断多人体验。

- **功能名称：联机失败自动回退**
  - **功能描述：**  
    系统会优先尝试联机网络；如果联机初始化失败，可按模式自动回退到本地可用状态（按当前配置策略）。
  - **使用场景：**  
    想要“能联机就联机，不能联机也先能用”，降低启动失败概率。

- **功能名称：产品化启动体验**
  - **功能描述：**  
    支持启动后自动打开页面、分发模式日志简化、便携运行时分发（免用户预装 Node）。
  - **使用场景：**  
    面向普通用户分发时，希望操作更接近“打开就用”的软件体验。

---

## 三、用户操作流程

下面按“第一次使用”的常见流程来走：

- **第 1 步：准备环境**
  - 双击 `setup.bat`，等待依赖安装完成。

- **第 2 步：启动服务**
  - 双击 `start.bat`。  
  - 程序会自动检查端口，必要时换一个可用端口启动。

- **第 3 步：进入页面**
  - 启动后在浏览器打开页面（若开启自动打开会直接弹出）。
  - 进入首页后可以看到默认房间信息与推荐分享链接。

- **第 4 步：分享给朋友**
  - 复制系统给出的分享链接，发送给朋友。
  - 朋友打开链接即可进入对应房间。

- **第 5 步：开始游戏**
  - 房主选择要玩的游戏（扫雷或德州扑克）。
  - 其他玩家按页面提示准备。
  - 房主点击开始，进入对局。

- **第 6 步：中途异常处理**
  - 如果有人临时断线，系统会在保留时间内允许重连恢复。
  - 如果房主离开，系统会自动切换新房主，房间继续可用。

- **第 7 步：结束与继续**
  - 一局结束后可继续下一局，或离开房间。
  - 所有人离开后，空房间会自动回收。

---

## 四、注意事项 / 环境要求

- **系统环境**
  - 推荐 Windows x64 环境使用（项目当前分发路径以 Windows 为主）。
  - 首次运行需要可用的 Node.js 与 npm（源码运行场景）。

- **分发运行**
  - 使用分发包时，建议通过 `launch-product.bat` 启动。
  - 分发目录需要带上便携运行时（`runtime/node.exe`），否则无法启动。

- **联机相关**
  - 若使用嵌入式联机能力，需要准备对应的 `libzt.dll` 文件并放到指定目录。
  - 网络环境不同会影响推荐分享地址与实际可达性，建议优先使用系统推荐地址。

- **使用限制**
  - 房主拥有关键控制权限（选游戏、开局等）。
  - 玩家断线恢复有时间窗口，超时后会按规则清理并需重新加入。

- **发布前建议**
  - 发布前先跑自动化检查（例如 `npm run release:check`）。
  - 再按手工回归清单进行关键流程验证，避免“能启动但体验异常”。
